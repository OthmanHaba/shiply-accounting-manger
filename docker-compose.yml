services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        image: account-app
        container_name: account-app
        restart: unless-stopped
        working_dir: /var/www/html
        ports:
            - "80"
        networks:
            - app-network

networks:
    app-network:
        driver: bridge
